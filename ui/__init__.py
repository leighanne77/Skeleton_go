"""ui — the user-facing Streamlit surface (rubric item 7, weighted equal to backend).

Two views of one run via a toggle: a plain-language Customer view (default) and an
Operator glass-box view. Built FIRST against a stub backend (T1); wired to the real
governed graph at T3+. See ui_build_prompt.md.
"""
