"""app.eval — the control-plane gate + (later) the golden harness.

The gate is the independent control node between the specialists and the ONE
synthesizer. The synthesizer is reachable only on the gate's pass edge.
"""
