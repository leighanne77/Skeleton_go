"""app.tools — in-process, swappable tools behind thin interfaces (design.md §2).

Keyless and offline by default; any keyed/live adapter is opt-in and never required
at runtime (CLAUDE.md principle 1).
"""
